# LocalStack

Init scripts and resource definitions for the emulated AWS services
(S3, SQS, SNS, SES, Secrets Manager, EventBridge) used locally. All
credentials used against LocalStack are the well-known fake `test`/`test`
pair — see [SECURITY.md](../../SECURITY.md).
