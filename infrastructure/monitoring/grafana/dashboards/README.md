# Grafana dashboards

Provisioned dashboard JSON, one per concern (bookings/minute, error rate,
latency, cache hit ratio, Kafka consumer lag, Mongo query performance).
Added alongside the observability baseline in Phase 1.
