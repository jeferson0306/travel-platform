# Kafka

Topic creation scripts and configuration for local Kafka (`docker-compose`
service defined in [infrastructure/docker](../docker)). Topics are declared
here as they are introduced — see ADR
[0004](../../docs/adr/0004-use-kafka-for-event-driven-communication.md).
