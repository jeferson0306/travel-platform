# Deployment

How each environment (local, staging demo) is deployed and configured,
including the target Kubernetes topology even though local development runs
on Docker Compose. Added as deployment pipelines are built — see
[ROADMAP.md](../../ROADMAP.md) Phase 4/6.
