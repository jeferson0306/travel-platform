# Runbooks

Operational playbooks: what to do when Kafka is down, Mongo is slow, Redis is
unavailable, the gateway is failing, or a queue is backing up. Written
against real failure scenarios exercised in chaos testing (Phase 4), not
speculatively.
