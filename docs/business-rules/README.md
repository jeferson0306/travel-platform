# Business rules

The domain rules behind each service — invariants, state machines, pricing
and cancellation policies — kept separate from the code so they can be
reviewed and understood without reading Java. One document per bounded
context, added as each domain is modeled.
