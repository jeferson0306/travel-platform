# Events

Human-readable catalog of domain events (what triggers them, what consumes
them, what happens on failure), complementing the machine-readable specs in
[docs/asyncapi](../asyncapi). Added as events are introduced.
