# backend

One directory per microservice, each an independent Maven module built on
Quarkus 3. See [ARCHITECTURE.md](../ARCHITECTURE.md) for the service map and
the package layout every service follows.

Services land here incrementally, per [ROADMAP.md](../ROADMAP.md), starting
with `identity-service`.
